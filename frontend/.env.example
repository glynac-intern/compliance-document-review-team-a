# The backend's base URL. NEXT_PUBLIC_ prefix is required for Next.js
# to expose this to client-side code (the API client runs in the
# browser, not just on the server) -- see TA-61.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
